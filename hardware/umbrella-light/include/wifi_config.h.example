/*
 * Copy this file to `include/wifi_config.h` and set your WiFi credentials.
 * The `include/wifi_config.h` file is gitignored by default so your
 * credentials won't be committed to source control.
 *
 * Example contents:
 *   #define WIFI_SSID "MyNetwork"
 *   #define WIFI_PASS "MySecretPassword"
 */

#ifndef WIFI_CONFIG_H
#define WIFI_CONFIG_H

// Uncomment and fill these lines in your local `include/wifi_config.h`:
// #define WIFI_SSID "YourSSID"
// #define WIFI_PASS "YourPassword"

#endif // WIFI_CONFIG_H
