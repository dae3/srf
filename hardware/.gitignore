# --- Toolchain & Binaries ---
# Ignore the manually downloaded xtensa compiler
.toolchain/

# --- Python (uv) ---
# Ignore the virtual environment created by uv
.venv/
# Ignore uv lock file (optional: keep it if you want deterministic builds)
# uv.lock

# --- direnv ---
# Ignore local environment overrides
.envrc.local
# If you use persistent direnv state
.direnv/

# --- ESP8266 SDK / Build Artifacts ---
# Ignore the build directory where binaries (.bin, .elf, .map) are generated
build/
# If using the older "make" system
SDK/build/
bin/

# Ignore configuration files generated by menuconfig
sdkconfig
sdkconfig.old

# --- OS / IDE Specific ---
.DS_Store
.vscode/
.idea/
